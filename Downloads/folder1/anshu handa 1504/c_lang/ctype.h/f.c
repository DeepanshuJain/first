#include<stdio.h>
int main()
{
	int i;
		printf("enter the character");
	scanf("%c",&i);
	if(i>=65&&i<=90)
		printf("character is in upper case");
	else
		printf("character is in lower case");
	return 0;
}
